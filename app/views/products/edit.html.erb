<div class="contain">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
      <h1 class="text-center">Editer le produit</h1>


      <%= render "form" %>

      <!--       <%= simple_form_for @product do |f| %>
        <%= f.input :name, placeholder: "Kudoz", label: "Which product?" %>
        <%= f.input :url, placeholder: "http://kudoz.com", label: "Which url?" %>
        <%= f.button :submit ,"Sauvegarder", class: "btn btn-primary" %>
      <% end %> -->

        <%= link_to "Cancel", product_path(@product), class: "btn btn-info" %>
      <!-- <form action="/products" method = "post">
        <div class="form-group">
          <label for="name">Choisissez un nom de produit</label>
          <input type="text" name="product[name]" class="form-control" placeholder="Kudoz" id="name">
        </div>
        <div class="form-group">
          <label for="url">Quelle url?</label>
          <input type="text" name = "product[url]" class="form-control" placeholder = "http://kudoz.com" id="url">
        </div>
        <input type="submit" value="Ajouter" class="btn btn-primary">
      </form> -->

    </div>
  </div>
</div>
